---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: cloudfoundry/garden-windows-ci

inputs:
- name: ci
- name: tracker-bot-repo

outputs:
- name: tracker-bot-repo

params:
  TRACKER_API_TOKEN:
  TRACKER_PROJECT_ID:

run:
  path: ci/tasks/deploy-tracker-bot/run
