---
platform: linux

image_resource: {type: docker-image, source: {repository: cloudfoundry/garden-windows-ci}}

inputs:
  - name: ci
  - name: bbl-state

outputs:
  - name: integration-config

run:
  path: ci/tasks/cats-integration-config/task

params:
  ADMIN_PASSWORD:
  ADMIN_USER:
  API:
  APPS_DOMAIN:
  UNALLOCATED_IP:
  WINDOWS_STACK:
  CREDHUB_MODE:
